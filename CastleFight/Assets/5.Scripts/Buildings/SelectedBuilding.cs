/*
 * This is the hit box for the barrack, so that once we tap at the barrack, that responsible barrack appear.
*/
using UnityEngine;
using System.Collections;

public class SelectedBuilding : MonoBehaviour {	
	//public Building parentBuilding;

	public virtual void isSeleted(){}

	public virtual void SP_isSelected(){
	}
}
